# Suggested X posts (Track 3)

Tag **@Telegraphprotoc**. Prefer a thread under an existing Track 1 miner post when relevant.

## Launch

```text
@Telegraphprotoc Track 3 preview — Signal Jury

A small app that asks the same forecast question to multiple live Telegraph miners in parallel and returns a jury brief (lean + dissent).

No mocks — real miner HTTPS chat endpoints.
Built to create demand on the supply layer (incl. groq-llama31-instant-miner).

#TelegraphProtocol #Track3
```

## Protocol alignment

```text
@Telegraphprotoc noted that non-response can cost a miner the whole epoch’s routing — silence is worse than a wrong answer.

Signal Jury surfaces miner successes and failures side-by-side so agents/humans see who actually answered.

#TelegraphProtocol
```
